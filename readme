# TinyLlama セットアップ手順書

この手順書では、TinyLlama-1.1B-Chat-v1.0 をローカルにダウンロードし、文章生成を行うまでの手順を示します。

---

## 使用環境

- Python 3.11

---

## 1. 仮想環境の作成

```bash
cd /Users/xxx/Documents/dev/
python3.11 -m venv llm-env
cd llm-env
source bin/activate
```

---

## 2. ライブラリのインストール

```bash
pip install --upgrade pip
pip install torch transformers==4.41.2
```

---
ディレクトリ作成
mkdir model


## 3. モデルのダウンロード

- 実行ファイル名: `dl/model_dl.py`

ここにダウンロードされているので、以下のディレクトリにコピーします

- 保存先: `/Users/xxx/Documents/dev/llm-env/model/`

以下のコマンドで実行します：

```bash
python3 dl/model_dl.py
```

---

## 4. 実行コード

- 実行ファイル名: `generate.py`
- 実行ファイル名: `mini.py`

---

## ディレクトリ構成例

```
llm-env/
├── model/             # TinyLlama の保存先
├── dl/
│   └── model_dl.py    # TinyLlama をダウンロードするスクリプト
├── generate.py        # 通常実行用
├── mini.py            # 最小構成実行用
```


